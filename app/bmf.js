'use strict';
class BestMusicFinder {
 constructor(album,n) {
     if (!album || !n)
        throw Error('Missing parameter error');
     this.album = album;
     this.n = n;
 }   
 
 addAlbum () {
     
 }
}

module.exports = BestMusicFinder;